# --- core api ---
fastapi~=0.115
uvicorn[standard]~=0.30

# --- config & utils ---
pydantic-settings~=2.3
python-dotenv~=1.0
loguru~=0.7

# --- data layer ---
sqlalchemy~=2.0
alembic~=1.13

# --- web fetch/parse ---
httpx~=0.27
beautifulsoup4~=4.12

# --- templating & pdf ---
jinja2~=3.1
playwright~=1.46
google-genai>=0.3


# --- optional: queues, db drivers (uncomment if/when you use) ---
# rq~=1.16
# redis~=5.0
# psycopg[binary]~=3.2   # only if switching to Postgres
# python-docx~=1.1      # only if you need DOCX output
# openpyxl~=3.1         # if you’ll write Excel directly from Python
